# Sleepwing 隐私政策

中文 | [English](PRIVACY.md)

生效日期：2026-07-29

Sleepwing 是一款本地优先的 macOS AI 编程助手陪伴工具。它不要求注册 Sleepwing
账户，也不会把提示词、代码、回复或健康行为发送到 Sleepwing 运营的服务器。

## Sleepwing 处理的数据

为了显示任务状态，Sleepwing 只接收平台名称、不透明会话标识、生命周期阶段、时间戳、
最多 48 个字符的可选任务名称及其类型，以及经过白名单校验的本地返回路径。会话
标识、任务名称和返回路径只用于当前任务视图，会随会话过期，不进入统计或诊断报告。

Sleepwing 会在本机保存应用设置、桌宠位置与外观、提醒反馈、有限的任务计时汇总、完成
任务数和聚合健康统计。提醒与运行历史最多保留 30 天。

Sleepwing 不保存完整提示词、源代码、助手回复、工具输入输出、错误正文或完整文件路径；
也不包含广告、跨应用跟踪、产品分析或遥测。

## 本地集成

Sleepwing 不发起任何对外网络连接——没有服务器端、没有遥测端点，也没有自动更新
检查。它唯一的网络行为是监听本机回环地址（`127.0.0.1`），已安装的钩子只把脱敏
后的事件发送到这个本地地址。随机接收令牌仅允许当前用户读取。

可选的桌宠对话默认完全本地（内置台词或系统端上模型）。只有当用户在设置中
明确选择「我的 Agent」时，输入的对话内容才会交给本机安装的 Agent CLI，并按照
该 Agent 服务商的隐私条款处理；Sleepwing 自身不会保存对话记录，也不会直接发起对外
网络连接。支持的 Agent 集成只有在用户主动操作后才会安装，也可以从 Sleepwing 中移除。
配置修改只针对 Sleepwing 自己管理的条目，并可能在原配置旁生成本地备份文件，以便恢复。

导入的 `.perchpet` 素材包保留在本机。Sleepwing 会校验大小、路径、清单、素材和视觉验收
摘要，并拒绝符号链接。已安装的旧版角色仍可继续使用，但未通过视觉验收的新角色不能安装。

## 通知与健康提醒

通知权限由 macOS 管理。Sleepwing 使用本地通知提供任务与健康提醒，并会在用户选择操作
后打开系统设置或受支持的平台应用。健康提示只是一般性的休息建议，不构成医疗建议，
Sleepwing 也不是医疗服务。

## 用户控制

你可以随时关闭通知、暂停提醒、断开集成或退出 Sleepwing。卸载前请先在 Sleepwing 中移除已
安装的集成，以便恢复由 Sleepwing 管理的配置。本地数据位于
`~/Library/Application Support/Perch`，应用偏好由 macOS 按
`app.sleepwing.Sleepwing` 标识保存。

安全问题请参阅 [SECURITY.md](SECURITY.md)。重大隐私变更会记录在
[CHANGELOG.md](CHANGELOG.md)。
