name: CI
on:
  push:
    branches: [main]
    paths: [Sources/**, Tests/**, Package.swift]
  pull_request:
  workflow_dispatch:
jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Run deterministic test suite
        run: swift test
